import { DivideValuesPipe } from './divide-values.pipe';

describe('DivideValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new DivideValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
